"""Tests for dependencies.py — auth dependency functions."""
import pytest


@pytest.mark.xfail(reason="TDD stub — write real failing test first", strict=True)
def test_something():
    raise NotImplementedError
