# Development + test dependencies for prana-ai
# Install alongside requirements.txt: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.14.0
httpx==0.27.2          # already in requirements.txt, listed here for clarity

# PDF rendering (required by stage03 + stage04 tests — same as PyMuPDF in requirements.txt)
PyMuPDF==1.24.10

# NSFW/CSAM stubs for unit tests (real model not needed)
nudenet==3.4.2

# Type checking
mypy==1.11.2
ruff==0.6.9
