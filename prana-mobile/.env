# PRANA API base URL
# Dev: point to local prana-api directly (bypasses Kong — dev only)
# Staging/Prod: set EXPO_PUBLIC_API_URL via EAS environment variables to the Kong URL
EXPO_PUBLIC_API_URL=http://192.168.29.126:8000
