// App.tsx — entry point for Expo Router
// Expo Router takes over from here via src/app/_layout.tsx.
// Font loading and providers are handled in the root layout.
export { default } from 'expo-router/entry';
