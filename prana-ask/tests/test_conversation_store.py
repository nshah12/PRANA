"""Tests for conversation_store.py — TDD stubs."""
import pytest


@pytest.mark.xfail(reason="TDD stub — write real failing test first", strict=True)
def test_conversation_stored_per_employee_not_global():
    raise NotImplementedError


@pytest.mark.xfail(reason="TDD stub — write real failing test first", strict=True)
def test_conversation_store_never_persists_raw_salary_or_pan():
    raise NotImplementedError
